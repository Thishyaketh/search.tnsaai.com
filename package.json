{
  "name": "searxng.org/devtools",
  "type": "module",
  "dependencies": {
  },
  "scripts": {
    "clean": "rm -Rf node_modules package-lock.json",
    "vercel-build": "pip install -r requirements.txt && pip install -r requirements-server.txt"
  }
}
